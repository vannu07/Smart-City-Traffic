# Smart City Traffic Management System - Environment Configuration
# Copy this file to .env and configure your settings

# === SERVER CONFIGURATION ===
ENVIRONMENT=development
DEBUG=True
HOST=0.0.0.0
PORT=5000

# === API CONFIGURATION ===
API_PREFIX=/api
API_VERSION=v1
CORS_ORIGINS=*

# === TRAFFIC SIMULATION SETTINGS ===
AUTO_REFRESH_INTERVAL=5
SIMULATION_SPEED=1.0
MAX_HISTORY_RECORDS=200

# === ML MODEL CONFIGURATION ===
CLUSTERING_ALGORITHM=kmeans
CLUSTERING_N_CLUSTERS=3
CLUSTERING_RANDOM_STATE=42

ANOMALY_ALGORITHM=isolation_forest
ANOMALY_CONTAMINATION=0.1
ANOMALY_RANDOM_STATE=42

ROUTE_ALGORITHM=shortest_path
ROUTE_WEIGHT_FACTOR=2.0

# === EXTERNAL API KEYS (Optional) ===
# Google Maps API - For real GPS and traffic data
# Get your API key from: https://developers.google.com/maps/documentation/javascript/get-api-key
GOOGLE_MAPS_API_KEY=

# OpenWeather API - For weather-based traffic analysis
# Get your API key from: https://openweathermap.org/api
OPENWEATHER_API_KEY=

# Traffic API - For real-time traffic data
# Various providers available (TomTom, HERE, MapBox, etc.)
TRAFFIC_API_KEY=

# === DATABASE CONFIGURATION ===
DATABASE_URL=sqlite:///traffic_data.db
DATABASE_ENABLED=False

# === LOGGING CONFIGURATION ===
LOG_LEVEL=INFO
LOG_FILE=logs/traffic_system.log

# === SECURITY CONFIGURATION ===
SECRET_KEY=your-secret-key-here-change-in-production
API_RATE_LIMIT=100/hour

# === FRONTEND CONFIGURATION ===
MAP_CENTER_LAT=40.7128
MAP_CENTER_LNG=-74.0060
MAP_ZOOM=13
FRONTEND_REFRESH_INTERVAL=10
FRONTEND_THEME=auto

# === NOTIFICATION CONFIGURATION ===
EMAIL_NOTIFICATIONS=False
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
ALERT_THRESHOLD=80.0

# === API SELECTION GUIDE ===
# 
# 1. GOOGLE MAPS API (Recommended for production)
#    - Provides real-time traffic data
#    - High accuracy GPS coordinates
#    - Requires billing account but has free tier
#    - Best for: Production deployments, real traffic data
#
# 2. OPENWEATHER API (Optional enhancement)
#    - Weather data affects traffic patterns
#    - Free tier available (1000 calls/day)
#    - Best for: Enhanced traffic predictions
#
# 3. TRAFFIC API (Alternative to Google Maps)
#    - TomTom, HERE, MapBox alternatives
#    - Different pricing models
#    - Best for: Cost optimization, specific regions
#
# 4. SIMULATION MODE (Default - No API key needed)
#    - Built-in traffic simulation
#    - Perfect for development and demos
#    - No external dependencies
#    - Best for: Development, testing, demos
#
# RECOMMENDATION:
# - Development: Use simulation mode (no API keys needed)
# - Production: Add Google Maps API key for real data
# - Enhancement: Add OpenWeather API for weather correlation